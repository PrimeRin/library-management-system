export enum Role {
    ADMIN = "ADMIN",
    STUDENT = "STUDENT",
    TEACHER = "TEACHER",
  }
  
  export enum Gender {
    MALE = "MALE",
    FEMALE = "FEMALE",
    OTHER = "OTHER",
  }
  
  export enum BorrowingStatus {
    ISSUED
    RETURNED
    OVERDUE
    RENEWED
    LOST
  }